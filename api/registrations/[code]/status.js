function json(body,status=200){return new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json'}})}
export default async function handler(req){const code=req.url.split('/').filter(Boolean).pop();const key=process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY;try{const r=await fetch(`${process.env.SUPABASE_URL}/rest/v1/registrations?code=eq.${encodeURIComponent(code)}&select=status,paid_at`,{headers:{apikey:key,Authorization:`Bearer ${key}`},signal:AbortSignal.timeout(8000)});const rows=await r.json();return rows[0]?json(rows[0]):json({error:'Not found'},404)}catch{return json({error:'Không thể kiểm tra trạng thái thanh toán.'},500)}}
